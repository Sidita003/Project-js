function addTeacher (teacherId, Name, surname, department, groupId) {
    let m;
    
    this.teacherId=teacherId;
    this.Name=Name;
    this.surname=surname;
    this.department=department;
    this.groupId=groupId;
        
    for(let i=n;i<n+m;i++){
        addTeacher[i].teacherId;
        addTeacher[i].Name;
        addTeacher[i].surname;
        addTeacher[i].department;
        addTeacher[i].groupId;
    }
    return n+m;
}